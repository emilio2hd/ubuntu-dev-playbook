---
- name: Update apt cache
  become: True
  apt: update_cache=yes

- name: Install apt dependencies
  become: True
  apt: name={{ item }}
  with_items:
    - autoconf
    - bison
    - build-essential
    - htop
    - libffi-dev
    - libssl-dev
    - libyaml-dev
    - python3-dev
    - python3-venv
    - tmux
    - tree
    - ttf-mscorefonts-installer
    - wget
